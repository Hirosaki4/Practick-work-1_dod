#include <iostream>
#include <Windows.h>
using namespace std;

int main()
{
    SetConsoleOutputCP(1251);
    SetConsoleCP(1251);
   
//Завдання 1
//Класифікація оцінок студента(від 0 до 100) і на основі цього значення виводить рівень успішності.
// Використайте конструкцію if - else для класифікації.
    
    /*int studentScore;
        cout << "Введіть оцінку студента (від 0 до 100): ";
        cin >> studentScore;

        if (studentScore >= 90 && studentScore <= 100) {
            cout << "Відмінно!" <<endl;
        }
        else if (studentScore >= 70 && studentScore < 90) {
            cout << "Добре! " <<endl;
        }
        else if (studentScore >= 50 && studentScore < 70) {
            cout << "Задовільно. " <<endl;
        }
        else {
            cout << "Незадовільно. " <<endl;
        }

        return 0;*/
      
    //Завдання 2
    //Напишіть програму, яка приймає два числа і визначає більше з них за допомогою тернарного оператора.
       
        /*int a, b;
        cout << "Введіть перше число: ";
        cin >> a;
        cout << "Введіть друге число: ";
        cin >> b;

        
        int max_number = (a > b) ? a : b;


        cout << "Більше число: " << max_number << endl;

        return 0;*/
    }
